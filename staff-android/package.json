{
  "name": "stayxpulse-staff-android",
  "version": "1.0.0",
  "private": true,
  "description": "StayXPulse Staff app — Capacitor shell that loads the live site, so web deploys update the app automatically",
  "scripts": {
    "sync": "npx cap sync android",
    "build": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.1",
    "@capacitor/cli": "^6.2.1",
    "@capacitor/core": "^6.2.1"
  }
}
